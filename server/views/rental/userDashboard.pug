extends user_layout.pug

block content
 .main.container  
    .jumbotron
          h1 Welcome to the AV Rental store
          br
          //- h3  #{user.email} is your login email
          br
          h3 Application main features
          p Login, register, contact, about and contact pages are rendered in pug.
          p Bootstraps and custom Style Sheets (CSS)
          p Using app.locals to pass variables to the view. 
          p Using a couple forms to process the encrypted form data. 
          form(method='GET', action='/rental/equipment')
           .form-group
             button.btn.btn-primary(type='submit') Proceed to Rental Shop